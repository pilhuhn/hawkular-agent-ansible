# Run the previously downloaded installer

---
- name: run installer
  command: java -jar /tmp/{{installer}} --module=/tmp/{{agent}} --wildfly-home=/tmp/{{wildfly}} --hawkular-server-url={{server_url}}


      
